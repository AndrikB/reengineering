package com.example.labyrinth;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

/*
public class LabyrinthGeneratorTest {

    private LabyrinthGenerator generatorSeeded0;
    private LabyrinthGenerator generatorSeeded1;
    private LabyrinthGenerator generatorSeeded2;
    private LabyrinthGenerator generatorSeeded3;
    private LabyrinthGenerator generatorSeeded4;

    {
        int x = 10;
        int y = 10;
        generatorSeeded0 = new LabyrinthGenerator(x,y, 5555);
        generatorSeeded1 = new LabyrinthGenerator(x, y, 1000);
        generatorSeeded2 = new LabyrinthGenerator(x, y, 2000);
        generatorSeeded3 = new LabyrinthGenerator(x * 2, y * 2, 9999);
        generatorSeeded4 = new LabyrinthGenerator(x * 2, y * 2, 9);
    }

    @Test
    public void getMatrix() {
        Labyrinth labyrinth = generatorSeeded0.getLabyrinth();

        assertEquals(labyrinth.getSize().y, 10);
        assertEquals(labyrinth.getSize().x, 10);
    }

    @Test
    public void getXExitPoint() {
        assertEquals(generatorSeeded0.getExitPoint().x, 9);
        assertEquals(generatorSeeded1.getExitPoint().x, 4);
        assertEquals(generatorSeeded2.getExitPoint().x, 0);
        assertEquals(generatorSeeded3.getExitPoint().x, 6);
        assertEquals(generatorSeeded4.getExitPoint().x, 19);
    }

    @Test
    public void getYExitPoint() {
        assertEquals(generatorSeeded0.getExitPoint().y, 1);
        assertEquals(generatorSeeded1.getExitPoint().y, 0);
        assertEquals(generatorSeeded2.getExitPoint().y, 3);
        assertEquals(generatorSeeded3.getExitPoint().y, 19);
        assertEquals(generatorSeeded4.getExitPoint().y,3);
    }

}
*/
